# -*- coding: utf-8 -*-
"""용연 플록큐레이터 교반강도(G값)·RPM 산정 설정 (덕남 이식본).

모델식: G = K0 · √(ρ(T)/μ(T)) · rpm^1.5

캘리브레이션 — 용연 기술진단서(제2장)는 계수가 1.21배 다른 두 표를 제시하며,
각각 내부적으로 수온·단별 일관(둘 다 G∝rpm^1.5 확인)하고 주변속도(유효반경)는
동일하다. 용도가 달라 **이원 캘리브레이션**을 채택한다:
  - K0_DIAG (지별 진단, 실측 rpm→G): 표 2.3-10/11.
    앵커 15℃·#1지 1단 1.82rpm ↔ G 36.1/sec → K0≈0.01570.
    현재 운영 상태(rpm→G) 재현·기준 판정·Gt 산정에 사용.
  - K0_STD (표준운영 조견표, 목표 G→권장 rpm): 표 2.3-12.
    앵커 15℃ 1단 1.99rpm ↔ G 50/sec → K0≈0.01900.
    목표 G 달성 권장 rpm 조견표 산출에 사용(진단서 공식 조견표 재현).
두 표의 1.21배 차는 저항·유효체적 파라미터를 다르게 잡은 진단서 내부 산정
차이이며, 본 모듈은 각 산출물을 해당 표와 일치시키기 위해 계수를 분리한다.
"""
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
IN_PARQUET = ROOT / "dataset" / "preprocessing_data" / "용연_전처리완료.parquet"
OUTPUT_DIR = ROOT / "05_mixing_gvalue" / "output"
IMAGES_DIR = OUTPUT_DIR / "images"

COL_TEMP = "원수 온도"   # 플록형성지 수온 대용 (원수 온도)

# ── 이원 캘리브레이션 앵커 (수온℃, rpm, G) ────────────────────────
# 지별 진단용 (표 2.3-10 #1지 1단, 실측 rpm→G): rpm→G·Gt 계산에 사용
CAL_DIAG = (15.0, 1.82, 36.1)
# 표준운영 조견표용 (표 2.3-12 15℃ 1단, 목표 G→rpm): 권장 rpm 산출에 사용
CAL_STD = (15.0, 1.99, 50.0)

# 주변속도 유효반경: 두 표 공통 v/rpm ≈ 18.35 (표 2.3-10 33.43/1.82,
# 표 2.3-12 36.50/1.99)에서 역산
R_EFF_M = 1.754

# ── 목표·기준 ──────────────────────────────────────────────────────
G_TARGETS = {1: 50.0, 2: 30.0, 3: 10.0}   # 단별 점감 목표 (Kawamura)
GT_RANGE = (3.0e4, 2.0e5)                  # Gt 적정 범위
G_STAGE3_MIN = 10.0                        # 3단 하한

# ── 플록형성지 제원 (진단서 표 2.3-1, 2.3-6) ───────────────────────
# 지 기하체적 B17.6 × L14.6 × H4.0 = 1,027.8㎥ — 진단서 체류시간(평균 37.5분)
# 및 Gt(6.3×10⁴ 등)와 정합. 표의 "776㎥/지 × 12지" 표기는 덕남 값이 잘못
# 기재된 것으로 판단(용연은 7지) — README 기록.
BASIN_VOL_M3 = 1027.8   # 지당 기하체적 (17.6×14.6×4.0)
N_STAGES = 3
N_BASINS_TOTAL = 7
FLOW_DESIGN = 315_000.0   # 설계유량 ㎥/일
FLOW_AVG = 276_540.0      # 평균유량
FLOW_MAX = 333_907.0      # 최대유량

# ── 수류혼화(착수정 낙차부) 제원 (진단서 표 2.2-3/4, 보완조사) ─────
HYD_HEAD_M = 0.58         # 보완조사 실측 낙차고 (전 7지 가동 시)
HYD_HRT_S = {             # 검토유량별 혼화시간(초, 설계값 0.58m 기준)
    "설계": 17.3, "평균": 19.7, "최대": 16.3,
}

# ── 진단서 지별 실측 회전수 (표 2.3-10/11, 7지) ────────────────────
BASIN_RPM = {
    "#1": (1.82, 1.59, 1.06),
    "#2": (1.84, 1.58, 1.04),
    "#3": (1.86, 1.49, 1.08),
    "#4": (1.87, 1.61, 1.08),
    "#5": (1.83, 1.56, 1.06),
    "#6": (1.93, 1.65, 1.22),
    "#7": (1.87, 1.48, 1.19),
}
